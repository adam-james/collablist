<h2>Your Playlists</h2>

<a href="<%= Routes.dashboard_playlist_path(@conn, :new) %>">New Playlist</a>

<ul>
<%= for playlist <- @playlists do %>
  <li>
    <%= playlist.name %>
    <a href="<%= Routes.dashboard_playlist_path(@conn, :show, playlist) %>">
      View
    </a>
  </li>
<% end %>
</ul>
